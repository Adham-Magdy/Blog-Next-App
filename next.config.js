/** @type {import('next').NextConfig} */
const nextConfig = {
    // Adding external domains
    images:{
        domains:[
            "images.pexels.com",
        ],
    }
}

module.exports = nextConfig
